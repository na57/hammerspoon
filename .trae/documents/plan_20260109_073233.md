## 实现计划

### 1. 分析当前结构
- 当前`index.html`是完整的HTML文件，包含所有内容和布局
- 项目已配置Jekyll，有`_config.yml`和`index.md`文件
- 现有`_layouts/default.html`布局与当前`index.html`结构差异较大

### 2. 实现步骤

#### 步骤1：创建新的Jekyll布局文件
- 将当前`index.html`复制为`_layouts/main.html`
- 保留完整的HTML结构、导航栏、侧边栏和页脚
- 将主内容区域（`<main class="main-content">...</main>`）替换为`{{ content }}`

#### 步骤2：修改index.html
- 将其转换为Jekyll模板文件
- 添加YAML Front Matter，指定使用新创建的`main`布局
- 删除硬编码的主内容，保留布局框架

#### 步骤3：配置index.md
- 确保`index.md`使用正确的布局
- 验证Markdown内容和include语句能正确渲染

#### 步骤4：调整资源引用
- 确保CSS和JavaScript引用使用Jekyll相对路径（`{{ '/css/styles.css' | relative_url }}`）
- 保持现有样式和交互功能不变

#### 步骤5：测试构建
- 运行Jekyll构建命令，验证生成的HTML文件保留原始布局和样式
- 确保所有内容正确从Markdown渲染

### 3. 预期结果
- 保留当前`index.html`的完整布局和样式
- 页面内容由`index.md`自动生成
- 支持Markdown格式和include语句
- 保持所有交互功能正常工作

### 4. 文件修改清单
- `index.html`：转换为Jekyll模板
- `_layouts/main.html`：新创建的布局文件，基于当前index.html
- 无需修改其他文件